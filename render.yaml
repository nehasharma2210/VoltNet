services:
  # Backend-only Docker service (Simplified)
  - type: web
    name: voltnet-backend
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 10000
      - key: PYTHONPATH
        value: /app